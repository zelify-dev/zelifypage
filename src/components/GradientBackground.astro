<div class="bg-gradient min-h-screen">
	<slot />
</div>

<style>
	.bg-gradient {
		background: #B0FF51;
		position: relative;
		overflow: hidden;
	}

	/* Removed overlay for solid background */

	.bg-gradient :global(*) {
		position: relative;
		z-index: 1;
		font-family: 'Nata Sans', sans-serif;
	}
</style>
